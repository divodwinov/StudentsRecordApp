<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- EditText for Kelas with increased touch target size -->
    <EditText
        android:id="@+id/kelas_edittext"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="@string/enter_nama"
        android:padding="16dp" />

    <!-- EditText for Nama with increased touch target size -->
    <EditText
        android:id="@+id/nama_edittext"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="@string/enter_kelas"
        android:inputType="textMultiLine"
        android:padding="16dp" />

    <!-- Button to add record with increased touch target size -->
    <Button
        android:id="@+id/add_record"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/add_record"
        android:minHeight="48dp"
        android:minWidth="48dp"
        android:padding="12dp" />
</LinearLayout>
